-- [[ UBY GOAT - LISFUS ENGINE FULL ]] --
-- STATUS: FIX X & K | ENGINE: LISFUS | SYNCED

local player = game.Players.LocalPlayer
local runService = game:GetService("RunService")
local userInputService = game:GetService("UserInputService")

-- -- 1. SCREENTROLL PINK -- --
local hijackGui = Instance.new("ScreenGui", player.PlayerGui)
hijackGui.Name = "UbyTroll"
hijackGui.DisplayOrder = 99999
local bg = Instance.new("Frame", hijackGui)
bg.Size = UDim2.new(1, 0, 1, 0)
bg.BackgroundColor3 = Color3.fromRGB(255, 230, 240) 
bg.Active = true

local msg = Instance.new("TextLabel", bg)
msg.Size = UDim2.new(0.8, 0, 0.4, 0)
msg.Position = UDim2.new(0.1, 0, 0.3, 0)
msg.BackgroundTransparency = 1
msg.Text = "PERANGKAT ANDA TELAH DI AMBIL ALIH\nBY UBY GOAT\n\nMampus kontol"
msg.TextColor3 = Color3.fromRGB(255, 80, 150)
msg.Font = Enum.Font.GothamBold
msg.TextSize = 30
msg.TextWrapped = true

local secretBtn = Instance.new("TextButton", bg)
secretBtn.Size = UDim2.new(0, 150, 0, 100)
secretBtn.Position = UDim2.new(0, 0, 1, -100)
secretBtn.BackgroundTransparency = 1
secretBtn.Text = ""
secretBtn.MouseButton1Click:Connect(function() hijackGui:Destroy() end)

-- -- 2. MENU UTAMA -- --
local screenGui = Instance.new("ScreenGui", player.PlayerGui)
screenGui.Name = "UbyMenu"
screenGui.ResetOnSpawn = false

local main = Instance.new("Frame", screenGui)
main.Size = UDim2.new(0, 220, 0, 280)
main.Position = UDim2.new(0.5, -110, 0.5, -140)
main.BackgroundColor3 = Color3.fromRGB(255, 235, 245)
main.Active = true
main.Draggable = true
Instance.new("UICorner", main)

-- TOMBOL X (KASIH POSISI LAGI)
local closeBtn = Instance.new("TextButton", main)
closeBtn.Size = UDim2.new(0, 25, 0, 25)
closeBtn.Position = UDim2.new(1, -30, 0, 5)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 120)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.new(1, 1, 1)
closeBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 5)
closeBtn.MouseButton1Click:Connect(function() screenGui.Enabled = false end)

local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, 0, 0, 50)
title.Text = "UBY GOAT <3"
title.TextColor3 = Color3.fromRGB(255, 50, 130)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 22

-- -- 3. LISFUS ENGINE LOGIC -- --
_G.HitboxStatus = false

local function toggleHitbox()
    _G.HitboxStatus = not _G.HitboxStatus
    local btn = main:FindFirstChild("TogBtn")
    
    if _G.HitboxStatus then
        if btn then btn.Text = "YAHAYUK: ON" btn.BackgroundColor3 = Color3.fromRGB(150, 230, 180) end
    else
        if btn then btn.Text = "AUTO WALK YAHAYUK" btn.BackgroundColor3 = Color3.fromRGB(255, 120, 180) end
        for _, v in pairs(game:GetService("Players"):GetPlayers()) do
            if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                pcall(function()
                    v.Character.HumanoidRootPart.Size = Vector3.new(2, 2, 2)
                    v.Character.HumanoidRootPart.Transparency = 0
                end)
            end
        end
    end
end

local btnH = Instance.new("TextButton", main)
btnH.Name = "TogBtn"
btnH.Size = UDim2.new(0.85, 0, 0, 50)
btnH.Position = UDim2.new(0.075, 0, 0.3, 0)
btnH.BackgroundColor3 = Color3.fromRGB(255, 120, 180)
btnH.Text = "AUTO WALK YAHAYUK"
btnH.TextColor3 = Color3.new(1,1,1)
btnH.Font = Enum.Font.GothamBold
Instance.new("UICorner", btnH)
btnH.MouseButton1Click:Connect(toggleHitbox)

local btnA = Instance.new("TextButton", main)
btnA.Size = UDim2.new(0.85, 0, 0, 50)
btnA.Position = UDim2.new(0.075, 0, 0.55, 0)
btnA.BackgroundColor3 = Color3.fromRGB(255, 160, 200)
btnA.Text = "UBY IMUT <3"
btnA.TextColor3 = Color3.new(1,1,1)
btnA.Font = Enum.Font.GothamBold
Instance.new("UICorner", btnA)
btnA.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Animation-Pack-FE-88221"))()
end)

-- -- 4. ENGINE SHORTCUTS (K & L) -- --
userInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.K then screenGui.Enabled = not screenGui.Enabled end
    if input.KeyCode == Enum.KeyCode.L then toggleHitbox() end
end)

runService.RenderStepped:Connect(function()
    if _G.HitboxStatus then
        for _, v in pairs(game:GetService("Players"):GetPlayers()) do
            if v ~= player and v.Character and v
